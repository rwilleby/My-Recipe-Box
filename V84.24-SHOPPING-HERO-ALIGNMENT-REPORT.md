# v84.24 Shopping List Hero Alignment

## Result

The Shopping List command strip now shares the large hero's centered desktop boundaries. Its complete one-row control set is 90% wide on desktop and 96% wide on smaller screens, matching the existing hero rules.

The individual control columns, segmented view selector, and four-counter group were reduced proportionally. Two-line labels and all existing behavior remain unchanged.

The former negative top margin was removed. The page's standard 18-pixel SECTION INTRO bottom gap now provides the requested breathing room between the supporting sentence and the button row.

## Verification

- `test/shoppingHeroAlignmentV8424.test.mjs`
- Existing v84.21, v84.22, and v84.23 Shopping List layout contracts

The focused contract and all three included Shopping List layout regression contracts passed. The production build and broader behavior suite were not run because this incremental update workspace does not contain installed dependencies or the complete application source tree.
