import assert from"node:assert/strict";import fs from"node:fs";const c=fs.readFileSync("src/App.css","utf8");const i=c.lastIndexOf("/* v73.8 — HOME Easy/Detailed video icon matches Quick Dinner Ideas */");assert.ok(i>=0);const f=c.slice(i);assert.ok(f.includes("width: 35px !important;"));assert.ok(f.includes("height: 35px !important;"));assert.ok(f.includes("width: 29px !important;"));console.log("v73.8 passed");