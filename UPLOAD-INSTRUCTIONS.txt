My Recipe Box v91.5 — catch-up package

Upload every file and folder inside this package to the repository root, preserving paths and replacing all matching files.

This package includes:
- Correct 1448 x 1086 divided-tray files for SD-027 through SD-053 in both directional side folders
- The responsive empty-tray partition component and styles
- The matching Meal Builder tests

This corrects the CI mismatch where GitHub retained the older 268 x 627 SD-027 Side 1 file.
Recipe-card hero images and their loader remain unchanged.
