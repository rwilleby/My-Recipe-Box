My Recipe Box v92.2 — changed files only

Upload the contents of this folder to the repository root, preserving paths and replacing matching files.

Build Your Own Meal refinements:
- Removed the tray canvas's unused top and bottom bands through internal cropping
- Top-aligned the interactive tray with the selector rows without shifting the whole left column
- Kept the 58px gutter between the interactive and selector areas
- Reallocated the recovered horizontal space to the selector area so all three selector columns are wider
- Confined ghosted Choose panels to the actual tray area
- Made the selected-dish preview button deselect that dish when clicked
- Added keyboard focus and hover feedback for deselection
- Updated all affected Meal Builder contracts

No recipe images or recipe-card hero loaders were changed.
